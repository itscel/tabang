declare global {
  namespace ReactNavigation {
    interface RootParamList {
      "/(auth)/sign-in": undefined;
      "/(auth)/sign-up": undefined;
      "/(app)": undefined;
      "/(app)/index": undefined;
    }
  }
}

// Keep this empty export to mark the file as a module
export {};
